#!/usr/bin/env bash
# USAGE: bin/release <build-dir>

BUILD_DIR=$1

cat <<-YAML
---
addons:
  - mongolab:sandbox
default_process_types:
  web: PATH=$HOME/.meteor:$PATH MONGO_URL=\$MONGOLAB_URI node app_build/bundle/main.js
YAML
